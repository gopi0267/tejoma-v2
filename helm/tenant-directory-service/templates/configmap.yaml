apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tenant-directory-service.fullname" . }}-config
  labels:
    {{- include "tenant-directory-service.labels" . | nindent 4 }}
data:
  PORT: "4003"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
