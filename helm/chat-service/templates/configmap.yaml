apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chat-service.fullname" . }}-config
  labels:
    {{- include "chat-service.labels" . | nindent 4 }}
data:
  PORT: "4006"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
