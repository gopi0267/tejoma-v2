apiVersion: v1
kind: Service
metadata:
  name: {{ include "jd-parser-service.fullname" . }}
  labels:
    {{- include "jd-parser-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "jd-parser-service.selectorLabels" . | nindent 4 }}
