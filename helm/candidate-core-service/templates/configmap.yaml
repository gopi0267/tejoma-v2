apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "candidate-core-service.fullname" . }}-config
  labels:
    {{- include "candidate-core-service.labels" . | nindent 4 }}
data:
  PORT: "4019"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
