apiVersion: v1
kind: Service
metadata:
  name: {{ include "candidate-core-service.fullname" . }}
  labels:
    {{- include "candidate-core-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "candidate-core-service.selectorLabels" . | nindent 4 }}
