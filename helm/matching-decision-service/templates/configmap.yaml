apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "matching-decision-service.fullname" . }}-config
  labels:
    {{- include "matching-decision-service.labels" . | nindent 4 }}
data:
  PORT: "4020"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
