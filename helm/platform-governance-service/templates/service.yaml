apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-governance-service.fullname" . }}
  labels:
    {{- include "platform-governance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "platform-governance-service.selectorLabels" . | nindent 4 }}
