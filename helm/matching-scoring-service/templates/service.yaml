apiVersion: v1
kind: Service
metadata:
  name: {{ include "matching-scoring-service.fullname" . }}
  labels:
    {{- include "matching-scoring-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "matching-scoring-service.selectorLabels" . | nindent 4 }}
