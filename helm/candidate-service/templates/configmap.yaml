apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "candidate-service.fullname" . }}-config
  labels:
    {{- include "candidate-service.labels" . | nindent 4 }}
data:
  PORT: "4005"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
