apiVersion: v1
kind: Service
metadata:
  name: {{ include "candidate-service.fullname" . }}
  labels:
    {{- include "candidate-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "candidate-service.selectorLabels" . | nindent 4 }}
