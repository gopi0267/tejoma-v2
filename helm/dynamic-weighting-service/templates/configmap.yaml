apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dynamic-weighting-service.fullname" . }}-config
  labels:
    {{- include "dynamic-weighting-service.labels" . | nindent 4 }}
data:
  PORT: "4017"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
