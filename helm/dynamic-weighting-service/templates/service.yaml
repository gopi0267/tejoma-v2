apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamic-weighting-service.fullname" . }}
  labels:
    {{- include "dynamic-weighting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dynamic-weighting-service.selectorLabels" . | nindent 4 }}
