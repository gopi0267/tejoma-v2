apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "matching-bge-shadow-service.fullname" . }}-config
  labels:
    {{- include "matching-bge-shadow-service.labels" . | nindent 4 }}
data:
  PORT: "4014"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
