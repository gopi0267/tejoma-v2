apiVersion: v1
kind: Service
metadata:
  name: {{ include "matching-bge-shadow-service.fullname" . }}
  labels:
    {{- include "matching-bge-shadow-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "matching-bge-shadow-service.selectorLabels" . | nindent 4 }}
