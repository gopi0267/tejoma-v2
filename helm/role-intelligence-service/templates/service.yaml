apiVersion: v1
kind: Service
metadata:
  name: {{ include "role-intelligence-service.fullname" . }}
  labels:
    {{- include "role-intelligence-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "role-intelligence-service.selectorLabels" . | nindent 4 }}
