apiVersion: v1
kind: Service
metadata:
  name: {{ include "matching-evaluation-service.fullname" . }}
  labels:
    {{- include "matching-evaluation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "matching-evaluation-service.selectorLabels" . | nindent 4 }}
