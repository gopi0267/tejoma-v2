apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "matching-evaluation-service.fullname" . }}-config
  labels:
    {{- include "matching-evaluation-service.labels" . | nindent 4 }}
data:
  PORT: "4011"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
