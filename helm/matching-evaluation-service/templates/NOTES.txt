matching-evaluation-service ({{ .Chart.AppVersion }}) deployed as {{ include "matching-evaluation-service.fullname" . }}.

Check rollout status:
  kubectl rollout status deployment/{{ include "matching-evaluation-service.fullname" . }} -n {{ .Release.Namespace }}

Check health:
  kubectl port-forward svc/{{ include "matching-evaluation-service.fullname" . }} 4011:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  curl http://localhost:4011/health

Reminder: this chart does NOT run database migrations. Before the first deploy to a new
environment, run matching-evaluation-service/scripts/migrate.ts (and validate-schema.ts) against
the target database out-of-band, then scripts/backfill-matching-evaluation-service.ts from the
monolith - see matching-evaluation-service/README.md and MIGRATION_RUNBOOK.md.

Confirm JWT_SECRET matches the monolith's own exactly (401s otherwise). This service is a
deliberately partial extraction of the Matching domain - see README.md's "Why this batch is
scoped this narrowly" before assuming skill/role intelligence, career intelligence, reasoning, or
live scoring live here too.
