apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recruiting-service.fullname" . }}-config
  labels:
    {{- include "recruiting-service.labels" . | nindent 4 }}
data:
  PORT: "4009"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
