apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "matching-reasoning-service.fullname" . }}-config
  labels:
    {{- include "matching-reasoning-service.labels" . | nindent 4 }}
data:
  PORT: "4012"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
