apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "career-intelligence-service.fullname" . }}-config
  labels:
    {{- include "career-intelligence-service.labels" . | nindent 4 }}
data:
  PORT: "4016"
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
