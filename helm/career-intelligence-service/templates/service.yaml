apiVersion: v1
kind: Service
metadata:
  name: {{ include "career-intelligence-service.fullname" . }}
  labels:
    {{- include "career-intelligence-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "career-intelligence-service.selectorLabels" . | nindent 4 }}
